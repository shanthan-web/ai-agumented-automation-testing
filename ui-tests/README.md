# UI Tests

Lightweight Selenium-based UI test module. Tests run headlessly with the HtmlUnit driver so they work without a browser install.

## Running locally
- From the project root: `mvn -pl ui-tests test`
- Or from this module: `mvn test`
